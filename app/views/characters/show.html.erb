<h2><%= @character.name %></h2>
<%= image_tag(@character.photo_url) %>

<h3><%= link_to "Edit Character", edit_house_character_path(@character.house, @character) %></h3>
<h4><%= link_to "Destroy character", house_character_path(@character), method: :delete %></h4>
